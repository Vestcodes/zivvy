[project]
name = "zivvy_brand"
authors = [{ name = "Vestcodes", email = "contact@vestcodes.com" }]
description = "Zivvy by Vestcodes — brand overlay, legal pages, Polar billing, and plan gating (https://zivvy.xyz)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0"
erpnext = ">=15.0.0"
